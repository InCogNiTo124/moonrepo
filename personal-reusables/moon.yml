# yaml-language-server: $schema=../.moon/cache/schemas/project.json
tags:
  - "sveltekit"

layer: library
language: typescript

toolchain:
  default: "bun"

tasks:
  build:
    command: bun
    args:
      - "run"
      - "build"
    outputs:
      - "dist/**"
    options:
      merge: replace
