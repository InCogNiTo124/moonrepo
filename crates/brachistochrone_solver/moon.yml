$schema: https://moonrepo.dev/schemas/project.json
tags:
- rust
- wasm
tasks:
  build:
    command: wasm-pack build
    env:
      RUSTUP_TOOLCHAIN: "${RUSTUP_TOOLCHAIN}"
    outputs:
    - pkg/
project:
  description: Solver for the Brachistochrone problem
  title: brachistochrone-solver
layer: library
