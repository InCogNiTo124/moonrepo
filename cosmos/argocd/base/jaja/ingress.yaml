apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jaja
  annotations:
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.tls.certresolver: "letsencrypt"
    traefik.ingress.kubernetes.io/router.middlewares: "argocd-redirect-https@kubernetescrd,jaja-cow-txt-interceptor@kubernetescrd"
spec:
  ingressClassName: traefik
  rules:
    - host: jaja.msmetko.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jaja
                port:
                  number: 80
