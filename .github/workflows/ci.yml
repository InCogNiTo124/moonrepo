name: "Moon CI"
on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"
  workflow_dispatch:
    inputs:
      targets:
        description: "Targets to run"
        required: false
        default: ""
        type: string

jobs:
  ci:
    name: "CI"
    runs-on: "ubuntu-latest"
    permissions:
      contents: read
      packages: write
    env:
      # Applies to the OTP source build that setup-toolchain's auto-install
      # kicks off for jaja's "asdf:erlang" pin.
      KERL_CONFIGURE_OPTIONS: "--without-wx --without-javac --without-odbc --disable-debug"
    steps:
      - name: Set up musl
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-dev musl-tools

      # jaja pins Erlang via proto's asdf backend, which has no prebuilt and
      # compiles OTP from source with kerl. These are kerl's build deps; the
      # configure options drop the parts we never use (wx/javac/odbc) to keep
      # it to a single pass. Took ~52s locally.
      - name: Set up Erlang build dependencies
        run: sudo apt-get install -y build-essential autoconf m4 libncurses-dev libssl-dev

      - name: Login to GitHub Container Registry
        uses: docker/login-action@master
        if: github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch'
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: "actions/checkout@main"
        with:
          fetch-depth: 0

      - uses: "moonrepo/setup-toolchain@v0"
        with:
          auto-install: true
          auto-setup: true
          # Caches ~/.proto, so the source-built OTP survives between runs
          # instead of being recompiled every time.
          cache: true

      # Proto installs Rust via rustup, but setup-rust defaults to installing
      # targets on 'stable'. Pin the channel to match the version in
      # .moon/toolchains.yml so targets land on the correct toolchain.
      - uses: moonrepo/setup-rust@v1
        with:
          channel: "1.96.1"
          targets: x86_64-unknown-linux-musl,wasm32-unknown-unknown

      - run:
          "moon ${{ github.event.inputs.targets && github.event.inputs.targets || 'ci'}}"

      # docker-push has runInCI: false in tag-docker.yml to prevent moon ci
      # from pushing images on PRs (GITHUB_TOKEN has packages:write).
      # However, runInCI: false also blocks `moon run` in CI, so we push
      # the images that were already built by moon ci's `docker` task.
      - name: Push Docker images
        if: github.ref == 'refs/heads/main'
        run: |
          for IMAGE in $(docker image ls --filter 'reference=ghcr.io/incognito124/*' --format '{{.Repository}}:{{.Tag}}'); do
            echo "::group::Pushing ${IMAGE}"
            docker push "$IMAGE"
            echo "::endgroup::"
          done

      - uses: moonrepo/run-report-action@v1
        if: success() || failure()
        with:
          access-token: ${{ secrets.GITHUB_TOKEN }}

      - name: "Collect/retrospect on results"
        uses: "appthrust/moon-ci-retrospect@main"
        if: always()
